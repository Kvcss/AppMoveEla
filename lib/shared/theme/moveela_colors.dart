import 'package:flutter/material.dart';

class MoveElaColors {
  // Brand
  static const primary = Color(0xFFFF2C7D);

  // Light
  static const lightBg = Color(0xFFFFFFFF);
  static const lightOnBg = Color(0xFF16161A);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightOnSurface = Color(0xFF242436);

  // Dark (tons iguais ao Figma)
  static const darkBg = Color(0xFF1B1023);      // fundo roxo
  static const darkOnBg = Color(0xFFF5ECF3);    // textos sobre o fundo
  static const darkSurface = Color(0xFF23132E); // superfícies (cards, se usar)
  static const darkOnSurface = Color(0xFFF8F4F7);

  // Aux
  static const divider = Color(0xFFE8E3E7);
}
