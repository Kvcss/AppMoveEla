import 'package:flutter/material.dart';

class MoveElaColors {
  // Rosa principal (brand)
  static const primary = Color(0xFFFF2C7D); // rosa vibrante

  // Light
  static const lightBg = Color(0xFFFFF6F8);          // quase branco com leve rosa
  static const lightOnBg = Color(0xFF16161A);        // texto principal
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightOnSurface = Color(0xFF242436);

  // Dark (fundo roxo-night)
  static const darkBg = Color(0xFF1B1023);           // roxo escuro
  static const darkOnBg = Color(0xFFF5ECF3);         // texto
  static const darkSurface = Color(0xFF23132E);
  static const darkOnSurface = Color(0xFFF8F4F7);

  // Neutros/auxiliares
  static const divider = Color(0xFFE8E3E7);
}
